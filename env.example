# GitHub Personal Access Token
# Create one at: https://github.com/settings/tokens
# Required scopes: repo (for reading issues and cloning repos)
GITHUB_TOKEN=ghp_your_token_here

# Target GitHub organization to monitor
GITHUB_ORG=helius-labs

# Polling interval in minutes (default: 5)
POLL_INTERVAL_MINUTES=5

# Directory to store cloned repos (default: ~/.mind-agent/repos)
REPOS_DIR=~/.mind-agent/repos

# Directory for agent data (SQLite DB, logs)
DATA_DIR=~/.mind-agent/data
